"""
SUPERSEDED. This prototype has been folded into signal_audit.py as
`subset_sums`, with the negative controls promoted to permanent tests in
test_signal_audit.py section 10.

Kept only because the working directory is on a cloud-synced volume
where deletion fails with EPERM. Do not import it; it is not maintained
and will drift from the engine.

See REAL_DASHBOARDS.md section 8 for the full repair cycle.
"""
raise ImportError(
    "subset_sum_proto is superseded — import subset_sums from signal_audit")
